//Tipos básicos: number, string e boolean (lógico)
//Tipo boolean
let vaiRepetir: boolean;
vaiRepetir = true;

while(vaiRepetir)
{
    vaiRepetir = false;
}

//Podemos fazer tudo em javascript! Tipo any: qualquer coisa
let y;
y = 3;
y = "Oi";

//if-else: mesma coisa que Javascript
let z: number;
z = 40;
//and, or e not: respectivamente &&, || e !
if(z >= 0 && z <= 100)
{
    console.log("Está dentro do intervalo entre 0 e 100");
}
else
{
    console.log("Está fora");
}
